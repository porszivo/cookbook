import 'package:flutter/material.dart';

class EinkaufslisteScreen extends StatelessWidget {
  const EinkaufslisteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Einkaufslist'),
    );
  }
}
